class Solution {
public:
    long long maximumHappinessSum(vector<int>& h, int k) {
        int n = h.size();
        long long sum = 0;
        sort(h.begin(),h.end());

        for(int i=0; i<k; i++){
            sum += (h[n-i-1]-i > 0 ?(h[n-i-1]-i) : 0);
        }

        return sum;
    }
};
