class Solution {
public:
    vector<int> sumZero(int n) {
        vector<int> arr(n, 0);
        int cnt = 1;
        for (int i = n / 2 - 1; i >= 0; i--) {
            arr[i] = cnt;
            arr[n - i - 1] = cnt * (-1);
            cnt++;
        }

        return arr;
    }
};
