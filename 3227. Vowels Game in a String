class Solution {
public:
    bool doesAliceWin(string s) {
        return any_of(s.begin(), s.end(), [](char ch) {
                return (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u');
            }
        );

    }
};
