class Solution {
public:
    int findClosest(int x, int y, int z) {
        int xd = abs(z-x);
        int yd = abs(z-y);

        if(xd == yd){
            return 0;
        }
        else if(xd < yd){
            return 1;
        }
        return 2;
    }
};
